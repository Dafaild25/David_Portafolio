---
// props: name, color, icon
const { name, color, icon: Icon } = Astro.props;
---

<div class={`flex flex-col items-center p-4 rounded-lg shadow-md ${color} text-primary w-28 h-28 hover:scale-110 transition-all duration-300 ease-in-out`}>
  <Icon class="w-8 h-8 mb-2" />
  <span class="text-sm font-medium text-center">{name}</span>
</div>