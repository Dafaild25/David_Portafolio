---
const { id = "", class: customClass = "", showRadial = false } = Astro.props;
---

<section id={id} class={` relative w-full px-4 sm:px-6 lg:px-8 mx-auto  flex flex-col justify-center ${customClass}`}>
  {showRadial && (
    <div class="absolute inset-0 -z-10 w-screen h-full [background:radial-gradient(125%_125%_at_50%_10%,#000_40%,#63e_100%)]"></div>
  )}
  <slot />
</section>